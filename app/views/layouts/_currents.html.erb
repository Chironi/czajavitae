<div class="currents_container">

  <% @posts.each do |post| %>
    <h2><%= post.title %></h2>
    <%= post.date.strftime("%A, %b. %d %Y %I:%M%p") %>
    <p><%= post.content %></p>
    <hr>
  <% end %>

</div>
